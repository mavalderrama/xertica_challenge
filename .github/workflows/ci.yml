name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_call:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
          python-version: "3.14"
      - name: Install dev dependencies
        run: uv sync --frozen --group dev
      - name: Run ruff lint
        run: uv run ruff check .
      - name: Run ruff format check
        run: uv run ruff format --check .
      - name: Mypy type check
        run: uv run mypy compliance_agent/

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: lint
    defaults:
      run:
        working-directory: backend

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: compliance_db
          POSTGRES_USER: compliance
          POSTGRES_PASSWORD: compliance
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DJANGO_SETTINGS_MODULE: config.settings.local
      POSTGRES_HOST: localhost
      POSTGRES_DB: compliance_db
      POSTGRES_USER: compliance
      POSTGRES_PASSWORD: compliance
      # LLM provider — tests use mock LLM so no real API call is made,
      # but the import in dependencies.py still requires these vars to be set.
      LLM_PROVIDER: openai
      OPENAI_API_KEY: test-key-ci-no-real-calls
      # Langfuse — tests use dummy keys; no real Langfuse server is needed.
      LANGFUSE_HOST: http://localhost:3000
      LANGFUSE_PUBLIC_KEY: "pk-lf-test"
      LANGFUSE_SECRET_KEY: "sk-lf-test"
      USE_MOCK_BQ: "true"
      USE_MOCK_GCS: "true"

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
          python-version: "3.14"
      - name: Install dependencies
        run: uv sync --frozen --group dev
      - name: Run migrations
        run: uv run python -m django migrate --settings=config.settings.local
      - name: Run tests with coverage gate
        run: uv run pytest tests/ -x --cov=compliance_agent --cov-fail-under=60 --cov-report=xml
      - name: Upload coverage report
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
        continue-on-error: true
